import React from "react";
import QuoteHolder from "./QuoteHolder";

function MainFile() {
  return (
    <div>
      <section>
        <QuoteHolder />
      </section>
    </div>
  );
}

export default MainFile;
